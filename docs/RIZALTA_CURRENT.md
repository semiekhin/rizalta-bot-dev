# RIZALTA Bot — Текущий статус

## Версия: 2.4.5

**Дата обновления:** 23.01.2026

---

## Что сделано в этой версии

### Корпус 3 — КП-only патч (whitelist)

Временное решение до появления Корпуса 3 на ri.rclick.ru:

- **264 лота** (отфильтрованы < 23.5 м²)
- **282 планировки** загружены локально
- **Whitelist доступ** — только для указанных chat_id
- **Команда** `/corp3` — прямой доступ
- **Кнопка** "🔒 Корпус 3" в меню "По корпусу" (для whitelist)
- **Генерация КП** с адаптированной вёрсткой (уменьшенное изображение планировки)

### Whitelist
```python
CORP3_WHITELIST = {
    512319063,   # Sergio
    8000703751,  # Евгения, Жилищная Инициатива
}
```

---

## Текущая архитектура Корпуса 3
```
/opt/bot-dev/
├── data/
│   ├── corp3_units.json          # 282 лота из Excel
│   └── corp3_layouts/            # 282 планировки (JPG, ~600KB каждая)
│       ├── 1 ЭТАЖ/
│       ├── 2 ЭТАЖ/
│       └── ...8 ЭТАЖ/
├── handlers/
│   └── corp3.py                  # Handler с собственным KP генератором
└── config/
    └── settings.py               # CORP3_WHITELIST
```

---

## Известные особенности

1. **Планировки тяжёлые** (~600KB vs ~100KB у корпусов 1-2)
   - КП генерируется медленнее
   - Можно оптимизировать сжатием JPG (отложено)

2. **Изолированный handler** — не использует properties.db
   - Данные в JSON, не синхронизируются с парсером
   - При появлении на rclick — мигрировать на стандартную схему

3. **Mini App** — Корпус 3 пока только в боте
   - Вариант А (параметр `?corp3=1`) обсуждён, не реализован
   - Ждём полноценную интеграцию

---

## Что работает

| Функционал | DEV | PROD |
|------------|-----|------|
| Корпусы 1-2 (стандартно) | ✅ | ✅ |
| Корпус 3 (whitelist) | ✅ | ✅ |
| Генерация КП | ✅ | ✅ |
| Mini App | ✅ | ✅ |
| Секретарь | ✅ | ✅ |
| Фиксация | ✅ | ✅ |

---

## Следующие шаги

1. Ждём появление Корпуса 3 на ri.rclick.ru
2. Полноценная интеграция (парсер, properties.db)
3. Добавление в Mini App (без whitelist)
4. Удаление временного патча corp3.py
