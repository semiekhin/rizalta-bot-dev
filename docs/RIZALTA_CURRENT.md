# Текущий статус RIZALTA

📅 **Последняя сессия:** 27.01.2026
🏷️ **Версия:** 2.5.2

## ✅ Что сделано (27.01.2026)

### 1. Приветственное сообщение /start
- Обновлён текст в `handlers/menu.py` (строки 33-50)
- Новый формат: описание RIZALTA AI SYSTEM + список возможностей

### 2. Фильтрация новостей — только российская экономика
- Файл: `handlers/news.py`
- Новые RSS источники: Ведомости, Коммерсант Экономика, РБК Экономика
- Удалены: РИА Новости, Lenta, ТАСС (политика/крайм)
- Include keywords: цб, ставка, инфляция, недвижимость, туризм...
- Exclude keywords: политика, война, криминал, международка

### 3. Корпус 3 — управление лотами
- Синхронизация с Excel: 154 available, 128 sold
- Фильтрация по статусу в `handlers/corp3.py` (строка 44)
- Поиск по коду интегрирован в `app.py`
- Команда `/ca` для управления:
```
  /ca list        — список скрытых
  /ca hide А300   — скрыть лот
  /ca show А300   — показать лот
```

### 4. Исправлен баг "Сравнить с депозитом"
- Проблема: цена 1000₽ вместо реальной (40 млн)
- Причина: callback формат изменился (добавился building), парсер не обновили
- Файл: `app.py` (строки 986-993)
- Также унифицирован формат в `handlers/compare.py`

### 5. SQLite WAL mode
- Включен для всех баз: properties.db, secretary.db
- Команда: `PRAGMA journal_mode=WAL;`
- Снижает блокировки при параллельных запросах

### 6. Апгрейд сервера
- Было: 2 vCPU, 4 GB RAM
- Стало: 4 vCPU, 8 GB RAM
- Создан клон сервера (бэкап в Амстердаме)

### 7. ADMIN_IDS — список админов
- Было: `ADMIN_ID = 512319063`
- Стало: `ADMIN_IDS = [512319063, 8000703751]`
- Евгения (Жилищная Инициатива) может использовать /ca, /wl, /parse

### 8. Инструкция для риэлторов
- Создан файл для Telegram: RIZALTA_AI_TELEGRAM.txt
- Описание возможностей AI-помощника

## 🔄 Текущее состояние

- **PROD:** работает ✅ (4 CPU, 8 GB RAM)
- **DEV:** работает ✅
- **Watchdog:** работает ✅
- **Клон:** создан (Амстердам, резерв)

## 📊 Корпус 3 — статистика
- Всего лотов: 282
- Доступно: 154
- Продано: 128

## 🔜 Следующие задачи

1. 🟢 **Запуск для агентств** — 28.01.2026
2. 🟡 Миграция на российский сервер (образ → Москва)
3. 🟡 Новый бот @RIZALTA_AI_BOT (параллельная работа)
4. 🟡 Автотесты перед деплоем
5. 🟡 Удалить legacy код — `kp_generator.py`, `rizalta_v2/`

## ⚠️ ВАЖНО при деплое

После копирования `app.py` из DEV в PROD — исправить:
1. URL Mini App: убрать `?env=dev`
2. Путь corp3_units.json: `/opt/bot-dev/` → `/opt/bot/`
3. Путь БД: `/opt/bot-dev/` → `/opt/bot/`
