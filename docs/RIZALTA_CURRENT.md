# Текущий статус RIZALTA

📅 **Последняя сессия:** 26.01.2026
🏷️ **Версия:** 2.5.1

## ✅ Что сделано (26.01.2026)

### Whitelist Корпуса 3 в БД
- Новая таблица `corp3_whitelist` в SQLite
- `corp3.py`: функция `is_whitelisted()` читает из БД
- `kp.py`: использует `is_whitelisted` из corp3
- `app.py`: команда `/wl` для управления whitelist
- `settings.py`: удалён `CORP3_WHITELIST` (мёртвый код)

### Команда /wl (только админ 512319063)
```
/wl              — справка
/wl list         — показать всех
/wl add ID Имя   — добавить
/wl remove ID    — удалить
```

### Изменённые файлы
- `handlers/corp3.py` — чтение из БД
- `handlers/kp.py` — использует is_whitelisted из corp3
- `config/settings.py` — удалён CORP3_WHITELIST
- `app.py` — команда /wl

## 🔄 Текущее состояние

- **PROD:** работает ✅
- **DEV:** работает ✅
- **Watchdog:** работает ✅

## 🔜 Следующие задачи

1. 🟡 Удалить legacy код — `kp_generator.py`, `rizalta_v2/`
2. 🟡 Техдолг: рефакторинг источников данных
3. 🟢 Доработка Mini App

## ⚠️ ВАЖНО при деплое

После копирования `app.py` из DEV в PROD — исправить:
1. URL Mini App: убрать `?env=dev`
2. Путь БД: `/opt/bot-dev/` → `/opt/bot/`
