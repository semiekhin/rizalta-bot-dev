# RIZALTA BOT — Текущие задачи

## Статус: ✅ Стабильная версия
## Версия: v1.8.1

---

## 📅 Сессия 10.12.2025

### ✅ Что сделано

**Фикс бага КП (ГОТОВО):**
- Проблема: при выборе лота B310 28.7м² генерировалось КП для B310 39.6м²
- Причина: код лота не уникален (дубликаты в разных корпусах)
- Решение: изменили поиск с `code` на `area`
- Файлы: `handlers/kp.py`, `app.py`

**Кнопка "Показать все" в Расчётах (ГОТОВО):**
- Добавлена в ROI по площади и бюджету
- Добавлена в Рассрочку по площади и бюджету
- Файлы: `handlers/calc_dynamic.py`, `app.py`

**Синхронизация БД (ГОТОВО):**
- Скопирована БД из dev в prod
- Настроен cron автопарсинга:
  - 03:00 — prod (/opt/bot)
  - 06:00 — dev (/opt/bot-dev)

**Метазнания AI (ГОТОВО):**
- Добавлена информация о функциях бота
- Добавлены инструкции по использованию
- Добавлены критические правила (не обещать то, что не умеет)
- Файл: `data/rizalta_knowledge_base.txt`

### 🟢 Текущий статус

| Компонент | Статус |
|-----------|--------|
| Prod бот (@RealtMeAI_bot) | ✅ работает |
| Dev бот (@rizaltatestdevop_bot) | ✅ работает |
| КП генератор | ✅ исправлен |
| Расчёты (кнопка "Показать все") | ✅ добавлена |
| Автопарсинг БД | ✅ cron настроен |
| Метазнания AI | ✅ добавлены |

### 🔜 Следующие задачи

1. **Специалисты для календаря**
   - Получить реальные данные: ФИО, telegram_id, email

2. **Безопасность SSH (prod)**
   - Порт 22 → 2222
   - Отключить root по паролю (только SSH-ключ)
   - Настроить автообновления безопасности

### 📁 Изменённые файлы (10.12.2025)

- `app.py` — добавлены callback для "Показать все"
- `handlers/kp.py` — фикс поиска по area вместо code
- `handlers/calc_dynamic.py` — кнопка "Показать все" в ROI и рассрочке
- `data/rizalta_knowledge_base.txt` — метазнания о боте

---

## 📅 Предыдущие сессии

### 09-10.12.2025
- Инвестиционный калькулятор (формулы Excel, 11 лет)
- DOCX экспорт расчётов
- Уведомления о показах в Telegram-группу
- PDF генератор КП с логотипом
- Приветствие "RIZALTA AI SYSTEM"

### 09.12.2025 (утро)
- Dev-окружение /opt/bot-dev
- Парсер ri.rclick.ru (369 квартир)
- units_db.py — единый источник данных

### 08.12.2025
- Модуль новостей и инвест-дайджест
- Курсы валют ЦБ РФ, погода, авиабилеты

### 07.12.2025
- AI Function Calling (10 функций)
- Календарь бронирования
- Голосовое управление (Whisper)

---

## 🚀 Команды
```bash
# Dev бот
systemctl restart rizalta-bot-dev
journalctl -u rizalta-bot-dev -f

# Prod бот
systemctl restart rizalta-bot
journalctl -u rizalta-bot -f

# Если порт занят
systemctl stop rizalta-bot
fuser -k 8000/tcp
sleep 2
systemctl start rizalta-bot
```

---

## 🏗️ Архитектура
```
/opt/bot-dev/          — Dev окружение
  └── @rizaltatestdevop_bot (polling mode)
  
/opt/bot/              — Production
  └── @RealtMeAI_bot (webhook через Cloudflare)

Cron:
  03:00 — парсинг prod
  06:00 — парсинг dev
```
